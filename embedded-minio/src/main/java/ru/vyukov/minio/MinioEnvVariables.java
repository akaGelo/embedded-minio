package ru.vyukov.minio;

/**
 * Envelopment variables supported minio
 * @author gelo
 *
 */
public enum MinioEnvVariables {

	MINIO_ENABLE_FSMETA, 
	MINIO_TRACE, 
	MINIO_PROFILER, 
	MINIO_BROWSER, 
	MINIO_ACCESS_KEY, 
	MINIO_SECRET_KEY, 
	MINIO_CACHE_SIZE, 
	MINIO_CACHE_EXPIRY, 
	MINIO_MAXCONN, 
	MINIO_PROFILE_DIR

}
